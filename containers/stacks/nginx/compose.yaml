name: nginx

services:
  nginx:
    image: nginx:alpine
    restart: unless-stopped
    networks:
      - edge
    ports:
      - "127.0.0.1:8080:80"

networks:
  edge:
    name: edge
    external: true
